spring.application.name=msvc-products

# Agregamos la URL donde debe ir a buscar la configuración
spring.cloud.config.uri=http://localhost:8888

# Seleccionamos el perfil de configuración, valores permitidos: dev, default, prod
spring.profiles.active=default

# Indica que todos los endpoints de Spring Actuator estarán habilitados
#management.endpoints.web.exposure.include=refresh